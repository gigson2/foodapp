# React UI Skill

## Conventions

- Use TypeScript for all components and hooks.
- Keep route pages under `resources/js/pages`.
- Use shared layout shells for public, customer, and admin views.
- Use Tailwind utility composition through `cn()` and small reusable primitives.
- Build accessible keyboard-friendly interactions by default.
- Preserve the warm, premium restaurant design language.
